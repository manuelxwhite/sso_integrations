<p style="color: green"><%= notice %></p>

<%= render @sso_client %>

<div class="mx-auto">
  <%= link_to "Edit this client", edit_sso_client_path(@sso_client) %> |
  <%= link_to "Back to clients", sso_clients_path %>

  <%= button_to "Destroy this client", @sso_client, method: :delete, class: "text-red-600 hover:underline", data: { confirm: "Are you sure?" } %>
</div>
